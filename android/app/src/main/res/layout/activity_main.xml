<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F172A"> <!-- Deep slate matching the web app's night theme -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Optional: A placeholder for the sun logo in the top left of the web app -->
        <ImageView
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@android:drawable/ic_menu_day"
            app:tint="#F59E0B"
            android:layout_marginBottom="24dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Smart Solar\nMicrogrid"
            android:textSize="40sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:textAlignment="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Peer-to-Peer Energy Trading System"
            android:textSize="16sp"
            android:textColor="#94A3B8"
            android:textAlignment="center"
            android:letterSpacing="0.05"
            android:layout_marginBottom="48dp" />

        <!-- Upgraded to MaterialButton for the rounded amber web styling -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGetStarted"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="GET STARTED →"
            android:textColor="#0F172A"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="#F59E0B"
            app:cornerRadius="12dp" />

    </LinearLayout>
</RelativeLayout>